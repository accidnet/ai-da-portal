[project]
name = "portal-onl"
version = "0.1.0"
description = "Python backend for the portal"
readme = "README.md"
requires-python = ">=3.14.3"
dependencies = []

[project.scripts]
portal-onl = "portal_onl:main"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
